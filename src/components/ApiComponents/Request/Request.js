import { Button, Card, Divider, Table, Tag, Row, Col, Input } from "antd";
import React from "react";

const Request = ({ selectedEndpoint }) => (
  <Card title="Request" className="mb-40 content-card">
    <div>

    </div>
  </Card>
)

export default Request;