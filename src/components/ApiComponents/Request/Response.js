import { Button, Card, Divider, Table, Tag, Row, Col, Input } from "antd";
import React from "react";

const Response = ({ selectedEndpoint }) => (
  <Card title="Response" className="mb-40 content-card">
    <div>

    </div>
  </Card>
)

export default Response;